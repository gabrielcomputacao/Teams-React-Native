export declare global {
  namespace ReactNavigation {
    interface RootParamList {
      /* nome da rota / se tem parametro ou nao */
      groups: undefined;
      new: undefined;
      players: {
        group: string;
      };
    }
  }
}
